import React from "react";
import "../index.css";

function Hero() {
    return ( <
        div className = "hero" >
        <
        h1 className = "Heading" > Camelot Global < /h1> <
        p className = "Content" > Read Anywhere Anytime < /p> <
        button className = "button" > Call to Action < /button> <
        /div>
    );
}

export default Hero;